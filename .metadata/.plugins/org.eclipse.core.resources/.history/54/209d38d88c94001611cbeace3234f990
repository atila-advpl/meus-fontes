#include 'protheus.ch'
#include 'parmtype.ch'

#define ENTER Chr(13)+Chr(13)

user function test_advpl_function()
	Local aArea := GetArea()
	Local aLista := {}
	Local nCnt
	
	For nCnt := 1 to Len(aLista)
	
	Next nCnt


	RestArea(aArea)
return